decorator:
  datasource:
    p6spy:
      enable-logging: true

spring:
#  h2:
#    console:
#      enabled: true         # H2 Console? ???? ?? (H2 Console? H2 Database? UI? ????? ??)
#      path: /h2-console     # H2 Console? Path
#  # Database Setting Info (Database? H2? ???? ?? H2?? ?? ??)
#  datasource:
#    driver-class-name: org.h2.Driver    # Database? H2? ?????.
#    url: jdbc:h2:tcp://localhost/${H2_DB_PATH}
#    #    url: "jdbc:h2:mem:"
#    username: sa
#    password:

  jpa:
    hibernate:
      ddl-auto: update    # Data Definition Language, Application 실행 시 데이터 정의어 옵션
